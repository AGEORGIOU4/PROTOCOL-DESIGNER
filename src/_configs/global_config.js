export const COOKIES_EXP = 1 / 24 //hours
export const COOKIE_SAVE_BROWSER_EXP = 7 //days

export const IDLE_TIMER = 60000 * 30 // Timer for idle modal to pop up
export const MODAL_IDLE_TIMER = 60000 * 2 // Timer for idle modal to pop up

export const CHECK_SESSION_TIMER = 60000 * 30 // Timer for session checker to automatically refresh the token
export const UNAUTHORIZED_TIMER = 500 // TImer for redirecting to /landing after unauthorized access
